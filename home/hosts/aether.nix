# home/hosts/aether.nix — generated by setup-host.sh
{ config, pkgs, ... }: {
  imports = [ ../common/core.nix ];

  # aether-specific packages
  home.packages = with pkgs; [

  ];

  home.file.".config/niri/config.kdl".source = ./niri.kdl;
}
